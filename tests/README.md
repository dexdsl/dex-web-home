# Visual Snapshot Updates

When UI changes are intentional, refresh the golden snapshots with:

```bash
npm run test:visual -- --update-snapshots
```

Then review the new snapshot files before committing.
